[project]
name = "p9s-dev"
version = "0.0.0"
description = "Dev tooling for p9s (Proxmox VE setup scripts)"
requires-python = ">=3.11"
dependencies = ["proxmoxer>=2.0"]

[dependency-groups]
dev = ["ty>=0.0.1"]

[tool.uv]
package = false
